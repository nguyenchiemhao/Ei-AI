# Hướng dẫn sử dụng cho người dùng mới — tài liệu 2

## Đăng nhập

Nếu không tìm thấy kết quả phù hợp, hãy thử diễn đạt câu hỏi theo cách khác hoặc kiểm tra lại xem tài liệu đã được tải lên đúng không gian làm việc hay chưa.

Để tải tài liệu gốc về máy, chọn biểu tượng tải xuống bên cạnh tên tệp; tệp luôn được trả về dưới dạng đính kèm và không bao giờ được mở trực tiếp trong trình duyệt.

## Tải tài liệu lên

1. Nếu không tìm thấy kết quả phù hợp, hãy thử diễn đạt câu hỏi theo cách khác hoặc kiểm tra lại xem tài liệu đã được tải lên đúng không gian làm việc hay chưa.
2. Để tải tài liệu gốc về máy, chọn biểu tượng tải xuống bên cạnh tên tệp; tệp luôn được trả về dưới dạng đính kèm và không bao giờ được mở trực tiếp trong trình duyệt.
3. Nhấn nút Tải lên ở góc phải màn hình để chọn tài liệu cần đưa vào hệ thống; có thể chọn nhiều tệp cùng lúc nếu chúng thuộc cùng một nhóm hồ sơ.
4. Để tải tài liệu gốc về máy, chọn biểu tượng tải xuống bên cạnh tên tệp; tệp luôn được trả về dưới dạng đính kèm và không bao giờ được mở trực tiếp trong trình duyệt.

## Tìm kiếm nội dung

Kết quả trả về hiển thị tên tệp, vị trí đoạn văn trong tài liệu gốc và phần nội dung được trích dẫn, giúp người đọc kiểm chứng lại nguồn.

Để tải tài liệu gốc về máy, chọn biểu tượng tải xuống bên cạnh tên tệp; tệp luôn được trả về dưới dạng đính kèm và không bao giờ được mở trực tiếp trong trình duyệt.

Để bắt đầu, người dùng đăng nhập bằng địa chỉ thư điện tử công ty và mật khẩu đã được cấp, sau đó chọn không gian làm việc tương ứng với bộ phận của mình.

Khi trạng thái chuyển sang đã lập chỉ mục, người dùng có thể tìm kiếm nội dung bên trong tài liệu bằng cách gõ từ khóa hoặc đặt câu hỏi bằng tiếng Việt có dấu.

### Khi không tìm thấy kết quả

Kết quả trả về hiển thị tên tệp, vị trí đoạn văn trong tài liệu gốc và phần nội dung được trích dẫn, giúp người đọc kiểm chứng lại nguồn.

Nhấn nút Tải lên ở góc phải màn hình để chọn tài liệu cần đưa vào hệ thống; có thể chọn nhiều tệp cùng lúc nếu chúng thuộc cùng một nhóm hồ sơ.
